{% extends 'layout.html.twig' %}

 {% block  body %}
  <div class="row">
  <div class="col-sm-12">
  <div class="card">
   <div class="card-header">Total de consultas por especialidad</div>
   <div class="card-body">
       {{ form(form) }}
       <h5 class="card-title">Descripcion del reporte {{ date|date('d-M-Y') }} </h5>
       {{ form_end(form) }}
    <p class="card-text"></p>
     <div class="row">
      <div class="col-sm-12">
       {{ include ("reporte/_consultas.html.twig", { 'especialidades':especialidades}) }}
      </div>
      <a class="btn btn-secondary" href="{{ path('app_menu') }}">Menu Principal</a>
                
     </div>
   </div>
  </div>
  </div>
  </div>
  </div>
 {% endblock %}