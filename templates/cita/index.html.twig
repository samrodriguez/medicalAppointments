{% extends 'base.html.twig' %}

{% block title %}Cita index{% endblock %}

{% block body %}
    <h1>Cita index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for citum in citas %}
            <tr>
                <td>{{ citum.id }}</td>
                <td>{{ citum.fecha ? citum.fecha|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('cita_show', {'id': citum.id}) }}">show</a>
                    <a href="{{ path('cita_edit', {'id': citum.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cita_new') }}">Create new</a>
{% endblock %}
